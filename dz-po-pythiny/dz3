import random
n = random.randrange(1, 11)
while True:
    guess=int(input("УГАДАЙТЕ ЧИСЛО 1 до 10: "))
    if (n==guess):
        print ("Вы угадали")
        break
    elif(num>guess):
        print ("ЗАГАДАННОЕ ЧИСЛО БОЛЬШЕ ")
    else:
        print ("ЗАГАДАННОЕ ЧИСЛО МЕНЬШЕ")

import random
def random_password():
    n = random.randrange(7, 11)
    password=""
    for i in range (1, n+1):
        password+=chr(random.randrange(33, 126))
    print (f'Ваш пароль из {n} символов: {password}')

random_password()
